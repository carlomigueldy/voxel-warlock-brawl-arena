.opencode/
.DS_Store
node_modules/
dist/
.vite/
.vercel/
.vercel
.env
.env.local
.env*
!.env.example
supabase/.temp/
.mcp.json

# Parity harness evidence (scripts/parity.mjs) — regenerated per run, not
# committed; each P4 PR attaches the golden/candidate/diff PNGs it needs to
# its own PR description/comments instead.
test/parity/.output/
